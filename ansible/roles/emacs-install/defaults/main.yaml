---
# defaults file for emacs-install

requested_emacs_version: ""

# How many date-stamped daily "master" builds to keep under ~/.local.
# Older ones (emacs-master-YYYY-MM-DD-*) are pruned after a run. The
# undated legacy build (emacs-master-<suffix>) is never touched.
emacs_keep_daily_builds: 3
